module.exports = function( name ) {
    var parts = [
        "define(",
        "	'" + name.replace(/\\/g, "/") + "',",
        "	[",
		"		'spell/functions'",
		"	],",
		"	function(",
		"		_",
		"	) {",
		"		// all the codes belongs to here",
		"	}",
		")"
    ]

	return parts.join( "\n" )
}

